import React, {useEffect} from "react"


function Nav(props) {
    const {
        pages = [],
        thisCurrentPage,
        currentPage
    } = props

    useEffect(() => {
        document.title = capitalize
    })
}